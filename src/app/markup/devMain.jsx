import React from "react";
import ReactDOM from "react-dom/client";
import Page from "./page.jsx"; // ✅ Must exist in the same folder

ReactDOM.createRoot(document.getElementById("root")).render(<Page />);
